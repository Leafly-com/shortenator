# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in shorten_urls.gemspec
gemspec

gem 'bitly'
gem 'rake', '~> 12.0'
gem 'rspec', '~> 3.0'

group :test do
  gem 'vcr'
  gem 'webmock', '~> 3.5'
end
